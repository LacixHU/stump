---
title: Retro computer libraries
description: Catalog and play C64, ZX Spectrum, Amiga, and DOS disk/tape/program images in the browser
---

# Retro computer libraries

Stump can catalog single-file disk and tape images for classic computers and play them in the browser via WASM emulators. The server only serves files and optional firmware bytes—it does **not** run emulators or stream video.

## Supported formats (MVP)

| Platform     | Extensions                                         |
| ------------ | -------------------------------------------------- |
| Commodore 64 | `d64`, `t64`, `prg`, `g64`, `tap` (with path hint) |
| ZX Spectrum  | `tzx`, `z80`, `sna`, `tap` (with path hint)        |
| Amiga        | `adf`, `adz`                                       |
| DOS          | `img`, `ima`, `exe`, `com`, `dosz`                 |

These extensions are registered globally (like PDF). Create a library with type **Retro** so auto-metadata (when enabled) targets the right providers.

## Folder layout

Prefer nested folders (library pattern **Nested** is the default when you pick Retro):

```text
/Retro/
  C64/Last Ninja/Last Ninja Side A.d64
  C64/Last Ninja/Last Ninja Side B.d64
  Spectrum/Manic Miner/manic.tap
  Amiga/Lemmings/Lemmings.adf
  DOS/Doom/DOOM.EXE
```

Each game folder becomes a **series**; each disk/tape file becomes **media**. Multi-disk games share one series.

### Ambiguous `.tap` files

`.tap` is used by both C64 and Spectrum. Resolution order:

1. Path segments containing `c64` / `commodore` → C64; `spectrum` / `zx` / `sinclair` → Spectrum
2. Else inherit platform from other media in the series
3. Else default to **C64** (name folders clearly to avoid this)

## C64 emulator

In-browser C64 play uses **[c64-ready](https://www.npmjs.com/package/c64-ready)** (MIT; WASM based on c64.js / lvllvl). Assets are served from `/retro/c64/` (`c64.wasm`, audio worklet).

Supported load types in this core: **`.d64`**, **`.prg`**, **`.crt`** (`.t64`/`.g64` are treated as disk-like when possible). Click the canvas for keyboard focus; default input mode is mixed joystick + keyboard.

### Controls

- **Reset** (toolbar) performs a C64 hard reset, then restarts the game the same way it was started the first time. The inserted disk stays in the drive; it does not destroy the emulator.
- **Settings** (toolbar) sets input mode (`mixed` / `keyboard` / `joystick`), joystick port (1 or 2), and disk speed. Click the canvas once if audio is muted (browser autoplay).
  - **Instant** (default) reads the first program out of the `.d64` directory and writes it straight into C64 memory, so a game that would take one to two minutes to load off the emulated 1541 starts in well under a second. The disk stays in the drive, so games that load further data during play still work — those transfers are warped instead (roughly 10x; gameplay itself stays at original C64 speed). Disks whose first file is not a BASIC-startable program fall back to a normal warped `LOAD"*",8,1`.
  - **Authentic** is real 1541 timing, with no warping and no injection.
  - Disk speed does not apply to `.t64` tapes or bare `.prg` files: there is no drive involved, so they are always written straight into memory. The program is started with `RUN` when it loads at `$0801`, and with `SYS <load address>` otherwise — which is how most machine-code tape rips start.
- Arrow keys and Left Ctrl are the joystick in mixed/joystick mode. Letter keys type, including the one next to Shift (Y on QWERTZ, Z on QWERTY). A USB or Bluetooth **gamepad** is the same stick as the on-screen one: D-pad or left analog stick for directions, the bottom or right face button (A / Cross, or B / Circle) to fire. It works on desktop even when the overlay is hidden. Keyboard-only input mode ignores the pad so arrows stay keyboard keys.
- On-screen controls show on touch/narrow screens, or while editing the overlay. Desktop fullscreen with a keyboard does not show them.
  - The **joystick** is a single stick, not four direction buttons: press anywhere on its base and slide, and it keeps following the finger without needing a lift between directions. A diagonal holds both directions at once, as a real stick does. Separate `up`/`down`/`left`/`right` buttons are still available in the overlay editor for layouts that want them.
- **Virtual keyboard** (toolbar) docks the full C64 keyboard under the playfield, on desktop and touch alike. It is part of the fullscreen element, so it stays available in fullscreen.
  - `SHIFT` and `C=` are sticky one-shots: tap the modifier, then the key it applies to. `SHIFT LOCK` holds shift down until it is tapped again, as on the machine.
  - The shifted legend is printed above the unshifted one, so `SHIFT` + `1` gives `!`, `SHIFT` + `HOME` gives `CLR`, `SHIFT` + `F1` gives `F2`, and `SHIFT` + either cursor key reverses its direction.
  - The C64, Spectrum, Amiga and DOS each have a layout; the button does not appear for platforms that do not.
- Users with **Manage library** can **Edit overlay** (Settings): pick the joystick, fire, or any C64 key from the on-screen keyboard and drag them into place. Save writes series-folder `controls.json`.
- The disk list **inserts** a new image into the running session (no reboot, no auto `LOAD`/`RUN`). Use this for multi-disk games.

Optional overlay layout lives in a series-folder `controls.json` next to the disks:

```json
{
	"keys": [
		{ "id": "joystick", "x": 0.15, "y": 0.74 },
		{ "id": "fire", "x": 0.88, "y": 0.78 },
		{ "id": "runstop", "x": 0.42, "y": 0.92 }
	]
}
```

`x` / `y` are 0–1 fractions of the playfield, and mark the centre of the control. `joystick` is the one id that is not a key — it is the stick itself, which resolves to the four direction keys as the finger moves. Older `{ "extraKeys": ["f1"] }` files still work (defaults plus extras). `restore` is not functional in this WASM build.

| Action                                  | Permission      |
| --------------------------------------- | --------------- |
| `GET /api/v2/media/{id}/retro-controls` | Library access  |
| `PUT /api/v2/media/{id}/retro-controls` | `ManageLibrary` |
| `GET /api/v2/media/{id}/save-state`     | Library access  |
| `PUT /api/v2/media/{id}/save-state`     | Library access  |
| `DELETE /api/v2/media/{id}/save-state`  | Library access  |

## ZX Spectrum emulator

In-browser Spectrum play uses **[JSSpeccy 3.2](https://github.com/gasman/jsspeccy3)** by Matt
Westcott: a WASM Z80 core in a Web Worker, cycle-accurate down to multicolour effects, with AY
and beeper audio. Assets are served from `/retro/spectrum/` (`jsspeccy.js`, the worker, the
core, ROMs and tape loaders).

JSSpeccy is **GPL-3.0**, unlike the rest of Stump (MIT), and is redistributed here under that
licence — its full text sits next to the bundle in `packages/browser/public/retro/spectrum/`.
Nothing but `emulators/spectrum.ts` touches it, so swapping cores is a one-file change.

Supported load types: **`.tap`** and **`.tzx`** tapes, **`.z80`** and **`.sna`** snapshots.
(The core also reads `.szx`, but that extension is not yet catalogued as retro media, so no
library will hand it to the player.) The Spectrum ROMs are bundled — Amstrad permits
redistributing them with an emulator — so unlike the Amiga there is nothing to install.

### Controls

- **Machine** (Settings) picks **Spectrum 128K** (the default), **48K** or **Pentagon 128**.
  Switching restarts the machine and reloads the game, so pick it before playing. 128K is the
  safer default for later titles; a few early games want a real 48K. A `.z80` or `.sna`
  snapshot carries its own machine type, so it always comes back as whatever it was saved on.
- **Loading speed** (Settings) is the tape equivalent of the C64's disk speed. **Instant**
  (default) traps the ROM's load routine and copies the tape in; **Authentic** plays the tape
  through the ULA at real speed, loading screens and all.
- **Joystick keys** (Settings). A Spectrum has no joystick port, and this core does not
  emulate a Kempston interface, so the on-screen stick and a gamepad press keys instead.
  Pick the scheme the game was written for:
  - **Q/A/O/P + Space** (default), the usual keyboard-game layout
  - **Cursor keys**, which are CAPS SHIFT + 5-8 with `0` to fire (Protek / AGF)
  - **Sinclair (6-0)**, the Interface 2 right-hand stick
- **Reset** (toolbar) restarts the machine and reloads the tape or snapshot.
- On-screen controls default to the stick, a fire button, `1`, `2` and `ENTER` — `1` and `2`
  pick a control method in a good half of the catalogue. Any of the machine's 40 keys can be
  dragged in from the overlay editor.
- **Virtual keyboard** (toolbar) docks the 48K rubber keyboard. `CAPS SHIFT` and `SYMBOL
SHIFT` are sticky one-shots that are genuinely held down across the next keystroke, because
  that is what they are on the machine: a game reading the cursor keys is reading CAPS SHIFT
  and `5` pressed together. The red SYMBOL SHIFT legend is printed above each key, so `SYM` +
  `P` types `"`.
- A physical keyboard is mapped by position (so a non-QWERTY layout still lines up), with
  Shift as CAPS SHIFT, Ctrl as SYMBOL SHIFT, and the keys a Spectrum never had — arrows,
  Backspace, Escape — sent as the CAPS SHIFT combinations they correspond to.

Overlay `controls.json` works exactly as it does for the C64, with `capsshift` and
`symbolshift` joining the vocabulary. A layout saved for a C64 game still loads: `runstop`
becomes BREAK, `instdel` becomes DELETE, the cursor ids become their CAPS SHIFT pairs, and
keys with no Spectrum equivalent (`commodore`, the function keys) simply do nothing.

Save states are not available: the bundle can load a snapshot but has no way to write one, so
the header's save and load buttons are hidden for Spectrum games.

## Play vs download

| Action                                                     | Permission                                   |
| ---------------------------------------------------------- | -------------------------------------------- |
| **Play** (`GET /api/v2/media/{id}/play-file`)              | Library access only (same as reading a page) |
| **Overlay keys** (`GET /api/v2/media/{id}/retro-controls`) | Library access only                          |
| **Save overlay** (`PUT /api/v2/media/{id}/retro-controls`) | `ManageLibrary`                              |
| **Save state** (`*/api/v2/media/{id}/save-state`)          | Library access only (each user's own save)   |
| **Download** (`GET /api/v2/media/{id}/file`)               | `DownloadFile`                               |

`play-file` only allows retro extensions (defense in depth so non-download users cannot pull EPUBs/CBZs through that path).

## Amiga emulator

In-browser Amiga play uses **[vAmigaWeb 4.3.6](https://github.com/vAmigaWeb/vAmigaWeb)** (GPL-3.0; WASM core of [vAmiga](https://dirkwhoffmann.github.io/vAmiga/)). Assets are served from `/retro/amiga/` (`vAmiga.js`, `vAmiga.wasm`). Kickstart ROMs are **not** bundled.

Supported load types: **`.adf`** and **`.adz`**. The machine is an A500 (512K chip + 512K slow, OCS).

### Firmware (Kickstart)

1. Set `firmware_dir` in `Stump.toml`, or `STUMP_FIRMWARE_DIR` (default: `{config_dir}/firmware`).
2. Place both files as those exact basenames (no subfolders):
   - `kick33180.A500` — Kickstart **1.2** (v33.180)
   - `kick34005.A500` — Kickstart **1.3** (v34.005)
3. The player fetches them via `GET /api/v2/firmware/{file_name}` (authenticated; basename only—no path traversal).

If firmware is missing, the catalog still works; Play shows setup instructions.

### Controls

- **Machine** (Settings) picks **Kickstart 1.3** (default) or **1.2**. Switching restarts the machine and reloads the disk.
- **Joystick port** (Settings) defaults to **port 2**, which is where almost every game expects the stick. Port 1 is the mouse.
- **Loading speed**: **Instant** (default) warps while the floppy is busy; **Authentic** is real disk timing.
- Arrow keys and Left Ctrl are the joystick in mixed/joystick mode. A gamepad is that same stick (D-pad or left analog, A / Cross to fire) on the selected port. The mouse on the canvas drives port 1.
- **Reset** hard-resets the A500 with the current disk still in DF0.
- Multi-disk titles: pick a sibling in the disk list to insert it without rebooting.
- **Virtual keyboard** (toolbar) docks the A500 keyboard (no numeric keypad). `SHIFT`, `ALT`, `Amiga` and `CTRL` are sticky one-shots held down across the next keystroke — vAmiga reads raw key codes, not a shift flag. Shifted legends are printed above each key. `Caps Lock` is a tap, which is how the machine locks it.
- Save states are available (vAmiga snapshots).

## DOS emulator

In-browser DOS play uses **[js-dos 6.22](https://js-dos.com)** / **[em-dosbox](https://github.com/dreamlayers/em-dosbox)** (GPL-2.0). Assets are served from `/retro/dos/` (`js-dos.js`, `wdosbox.js`, `wdosbox.wasm.js`). No IBM BIOS is required.

Supported load types:

| Extension       | What happens                                                                                        |
| --------------- | --------------------------------------------------------------------------------------------------- |
| `.img` / `.ima` | Floppy image mounted as `A:`                                                                        |
| `.exe` / `.com` | Written to `C:` and run                                                                             |
| `.dosz`         | Zip bundle extracted to `C:`; `AUTOEXEC.BAT` / `START.BAT` / a matching EXE is started when present |

Do **not** use `.zip` — that extension is already comic/CBZ. Rename a game folder zip to `.dosz`.

Path segments `dos`, `msdos`, or `pc` tag a folder as DOS.

### Controls

- A physical keyboard types into DOSBox. The **virtual keyboard** is a PC 101 layout (no keypad); `SHIFT`, `CTRL` and `ALT` are sticky one-shots.
- The mouse on the canvas is the DOS mouse. On a phone, drag a finger to move the pointer (it is **not** under the finger); tap is left click; a still long-press is right click.
- On-screen stick and fire send arrow keys and Ctrl. Many DOS games accept that; mouse-only titles use the trackpad.
- Save states are not available for this core.

## Covers / thumbnails

Retro files have no internal pages, so Stump **cannot** generate a cover from the disk image itself.

**Automatic covers work when you provide a local image next to the game:**

| Location                     | Filenames                                                                                                              |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Same folder as the disk/tape | `{game name}.jpg` / `.png` / `.webp`, or `cover.jpg`, `folder.jpg`, `poster.jpg`, `front.jpg`, `box.jpg`, `boxart.jpg` |
| Parent (series) folder       | same `cover` / `folder` / … names                                                                                      |

Then run a library **scan** (or thumbnail generation). Stump copies/resizes the sidecar into the thumbnail store.

If no sidecar is found, Stump searches Wikipedia via the MediaWiki API in [Category:Video game covers](https://en.wikipedia.org/wiki/Category:Video_game_covers) (and platform subcategories such as Commodore 64 / ZX Spectrum / Amiga / MS-DOS). A matching cover is downloaded and stored as the thumbnail.

Sidecar files always win over Wikipedia. Many Wikipedia covers are fair-use; only use this for your own library. Manual upload on the book manage page still works.

## Save states

A save state is the complete memory image of the emulated machine — CPU, RAM, and chip
state — so restoring one drops you back exactly where you were, mid-level and all.

Save states are stored **on the server**, one per user per game:

- They follow you between devices and browsers. Save on the desktop, load on a tablet.
- They are private. Each user has their own slot for a game; no one else can read or
  overwrite it, and there is no shared or "server-wide" save.
- They survive clearing site data, since nothing is kept in the browser.

Use the **Save state** and **Load state** buttons in the player header. There is one slot
per game: saving again replaces the previous snapshot. For a multi-disk game, the slot
belongs to the book you opened, so swapping disks mid-game does not hide the save behind
a different disk.

Snapshots are written to `save_states/{media_id}/{user_id}.savestate` under the Stump
config directory, with a small metadata row in the database. Deleting a library removes
the snapshots belonging to its books. The server accepts snapshots up to **16 MiB**,
which is comfortably above a C64 snapshot (a few hundred KiB) and an Amiga one
(a few MiB).

Save states depend on the emulator core being able to write a snapshot. The C64 and Amiga
cores can; JSSpeccy and js-dos cannot write them, so the save and load buttons are hidden
for Spectrum and DOS games rather than offered and then refused.

## Legal

You must supply your own game images. Stump does not distribute copyrighted disk or tape
images.

Machine ROMs differ by platform. The Spectrum ROMs ship with the JSSpeccy bundle, which
Amstrad permits for emulators; the Amiga's Kickstart does not allow that, so you supply it
yourself (see above). DOSBox does not need an IBM BIOS. The Spectrum and Amiga cores are
GPL-3.0; the DOS core is GPL-2.0 — see those sections.

## OPDS

OPDS entries for retro media expose **file acquisition** only (no page stream links). Page-based OPDS reading does not apply when `pages = -1`.
