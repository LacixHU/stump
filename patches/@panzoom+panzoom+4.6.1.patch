diff --git a/node_modules/@panzoom/panzoom/dist/panzoom.es.js b/node_modules/@panzoom/panzoom/dist/panzoom.es.js
index 49eeb4c..1a97af3 100644
--- a/node_modules/@panzoom/panzoom/dist/panzoom.es.js
+++ b/node_modules/@panzoom/panzoom/dist/panzoom.es.js
@@ -712,20 +712,23 @@ function Panzoom(elem, options) {
             if (startDistance === 0) {
                 startDistance = getDistance(pointers);
             }
-            // Use the distance between the first 2 pointers
-            // to determine the current scale
-            var diff = getDistance(pointers) - startDistance;
-            toScale = constrainScale((diff * options.step) / 80 + startScale).scale;
-            zoomToPoint(toScale, current, { animate: false }, event);
+            // Use the ratio of finger distance to determine scale (1:1 with native pinch)
+            var currentDistance = getDistance(pointers);
+            toScale = startDistance > 0
+                ? constrainScale(startScale * (currentDistance / startDistance)).scale
+                : scale;
+            // Recompute from gesture start each frame so pinch stays under the fingers.
+            // zoomToPoint alone then pan(orig+delta) overwrites the focal adjustment and
+            // effectively zooms about the center whenever the midpoint is stable.
+            x = origX;
+            y = origY;
+            scale = startScale;
+            options.setTransform(elem, { x: x, y: y, scale: scale, isSVG: isSVG }, options);
+            zoomToPoint(toScale, { clientX: startClientX, clientY: startClientY }, { animate: false }, event);
+            // Follow finger-midpoint movement (native pinch pan)
+            pan(x + (current.clientX - startClientX) / toScale, y + (current.clientY - startClientY) / toScale, { animate: false }, event);
         }
-        // Pan during pinch if pinchAndPan is true.
-        // Note: some calculations may be off because the zoom
-        // above has not yet rendered. However, the behavior
-        // was removed before the new scale was used in the following
-        // pan calculation.
-        // See https://github.com/timmywil/panzoom/issues/512
-        // and https://github.com/timmywil/panzoom/issues/606
-        if (!hasMultiple || options.pinchAndPan) {
+        else {
             pan(origX + (current.clientX - startClientX) / toScale, origY + (current.clientY - startClientY) / toScale, { animate: false }, event);
         }
     }
diff --git a/node_modules/@panzoom/panzoom/dist/panzoom.js b/node_modules/@panzoom/panzoom/dist/panzoom.js
index 0bc6776..696beb5 100644
--- a/node_modules/@panzoom/panzoom/dist/panzoom.js
+++ b/node_modules/@panzoom/panzoom/dist/panzoom.js
@@ -718,20 +718,23 @@
                 if (startDistance === 0) {
                     startDistance = getDistance(pointers);
                 }
-                // Use the distance between the first 2 pointers
-                // to determine the current scale
-                var diff = getDistance(pointers) - startDistance;
-                toScale = constrainScale((diff * options.step) / 80 + startScale).scale;
-                zoomToPoint(toScale, current, { animate: false }, event);
+                // Use the ratio of finger distance to determine scale (1:1 with native pinch)
+                var currentDistance = getDistance(pointers);
+                toScale = startDistance > 0
+                    ? constrainScale(startScale * (currentDistance / startDistance)).scale
+                    : scale;
+                // Recompute from gesture start each frame so pinch stays under the fingers.
+                // zoomToPoint alone then pan(orig+delta) overwrites the focal adjustment and
+                // effectively zooms about the center whenever the midpoint is stable.
+                x = origX;
+                y = origY;
+                scale = startScale;
+                options.setTransform(elem, { x: x, y: y, scale: scale, isSVG: isSVG }, options);
+                zoomToPoint(toScale, { clientX: startClientX, clientY: startClientY }, { animate: false }, event);
+                // Follow finger-midpoint movement (native pinch pan)
+                pan(x + (current.clientX - startClientX) / toScale, y + (current.clientY - startClientY) / toScale, { animate: false }, event);
             }
-            // Pan during pinch if pinchAndPan is true.
-            // Note: some calculations may be off because the zoom
-            // above has not yet rendered. However, the behavior
-            // was removed before the new scale was used in the following
-            // pan calculation.
-            // See https://github.com/timmywil/panzoom/issues/512
-            // and https://github.com/timmywil/panzoom/issues/606
-            if (!hasMultiple || options.pinchAndPan) {
+            else {
                 pan(origX + (current.clientX - startClientX) / toScale, origY + (current.clientY - startClientY) / toScale, { animate: false }, event);
             }
         }
diff --git a/node_modules/@panzoom/panzoom/src/panzoom.ts b/node_modules/@panzoom/panzoom/src/panzoom.ts
index a1e74bd..5b0f646 100644
--- a/node_modules/@panzoom/panzoom/src/panzoom.ts
+++ b/node_modules/@panzoom/panzoom/src/panzoom.ts
@@ -480,21 +480,33 @@ function Panzoom(elem: HTMLElement | SVGElement, options?: PanzoomGlobalOptions)
       if (startDistance === 0) {
         startDistance = getDistance(pointers)
       }
-      // Use the distance between the first 2 pointers
-      // to determine the current scale
-      const diff = getDistance(pointers) - startDistance
-      toScale = constrainScale((diff * options.step) / 80 + startScale).scale
-      zoomToPoint(toScale, current, { animate: false }, event)
-    }
-
-    // Pan during pinch if pinchAndPan is true.
-    // Note: some calculations may be off because the zoom
-    // above has not yet rendered. However, the behavior
-    // was removed before the new scale was used in the following
-    // pan calculation.
-    // See https://github.com/timmywil/panzoom/issues/512
-    // and https://github.com/timmywil/panzoom/issues/606
-    if (!hasMultiple || options.pinchAndPan) {
+      // Use the ratio of finger distance to determine scale (1:1 with native pinch)
+      const currentDistance = getDistance(pointers)
+      toScale =
+        startDistance > 0
+          ? constrainScale(startScale * (currentDistance / startDistance)).scale
+          : scale
+      // Recompute from gesture start each frame so pinch stays under the fingers.
+      // zoomToPoint alone then pan(orig+delta) overwrites the focal adjustment and
+      // effectively zooms about the center whenever the midpoint is stable.
+      x = origX
+      y = origY
+      scale = startScale
+      options.setTransform(elem, { x, y, scale, isSVG }, options)
+      zoomToPoint(
+        toScale,
+        { clientX: startClientX, clientY: startClientY },
+        { animate: false },
+        event
+      )
+      // Follow finger-midpoint movement (native pinch pan)
+      pan(
+        x + (current.clientX - startClientX) / toScale,
+        y + (current.clientY - startClientY) / toScale,
+        { animate: false },
+        event
+      )
+    } else {
       pan(
         origX + (current.clientX - startClientX) / toScale,
         origY + (current.clientY - startClientY) / toScale,
